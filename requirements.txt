# 核心数据处理
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pyarrow>=10.0.0

# 数据获取
akshare>=1.10.0
tushare>=1.2.0
baostock>=0.8.0

# 技术分析（默认后端 pandas_ta，纯 Python 无需 C 依赖；TA-Lib 为可选增强，
# 需本地 C 库，与 SKILL.md FACTOR_BACKEND 默认 pandas_ta 一致）
pandas-ta>=0.3.0

# ===== 可选依赖（非必需，按需安装）=====
# TA-Lib：可选因子计算后端（FACTOR_BACKEND=talib 时启用），需先安装系统 C 库
# TA-Lib>=0.4.0

# 回测框架
backtrader>=1.9.0
vectorbt>=0.25.0

# 风险与组合管理
PyPortfolioOpt>=1.5.0
Riskfolio-Lib>=5.0.0
# cvxpy 固定到已验证兼容版本，规避 Windows 下原生扩展线程竞态导致的
# `Windows fatal exception: access violation`（OPEN-2026-017 残余根因之一）
cvxpy==1.9.2

# 可视化
matplotlib>=3.7.0
plotly>=5.14.0
quantstats>=0.9.0

# 因子分析（alphalens 原版停止维护，使用 alphalens-reloaded 兼容 pandas 2.0）
alphalens-reloaded>=0.4.5

# 高性能 DataFrame 后端（可选，IC/中性化等热路径加速 5-15×）
polars>=0.20.0

# 机器学习
scikit-learn>=1.3.0
lightgbm>=3.3.0
catboost>=1.2
# 超参优化与实验追踪（strategy-model-engine 可选；未安装时自动降级跳过）
optuna>=3.0.0
mlflow>=2.0.0
joblib>=1.3.0

# 模板渲染（reports-engine 报告 HTML 必需）
jinja2>=3.1.0

# 工具库
python-dateutil>=2.8.0
pytz>=2023.3
